name: Deploy to EC2

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Login to docker hub
        uses: docker/login-action@v2 # More secure than using -u -p
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/nodejs-app:${{ github.sha }} -t ${{ secrets.DOCKER_USERNAME }}/nodejs-app:latest .

      - name: Publish image to docker hub
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/nodejs-app:${{ github.sha }}
          docker push ${{ secrets.DOCKER_USERNAME }}/nodejs-app:latest

  deploy:
    needs: build
    runs-on: [ip-172-31-0-248]
    steps:
      - name: Pull image from docker hub
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/nodejs-app:${{ github.sha }}

      - name: Delete old container
        run: docker rm -f nodejs-app-container || true # Won't fail if container doesn't exist

      - name: Run docker container
        run: |
          docker run -d \
            -p 3000:3000 \
            --name nodejs-app-container \
            --restart unless-stopped \
            ${{ secrets.DOCKER_USERNAME }}/nodejs-app:${{ github.sha }}

      - name: Clean up old images
        run: docker system prune -f
